name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: npm install

          
   
  build2:
    name: Run API-2
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Build API
        run: |
          echo "Build API"
          sleep 10s
